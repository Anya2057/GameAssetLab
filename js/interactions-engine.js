function bindInteractions(root,getConfig,burst,notify){let test=false;const layer=()=>root.querySelector('.anim-layer');root.addEventListener('mouseenter',()=>{if(!test)return;const config=getConfig(),h=config.interactions.hover;root.style.transition='transform .18s,filter .18s';root.style.filter=`brightness(${h.brightness||100}%) drop-shadow(0 0 ${h.glow||0}px #9d7cff)`;root.style.transform=`translate(${config.transform.x}px,${config.transform.y}px) scale(${config.transform.scale*(h.scale||1)}) rotate(${config.transform.rotation+(h.rotate||0)}deg)`;if(h.shake)layer().animate([{transform:'translateX(-4px)'},{transform:'translateX(4px)'},{transform:'none'}],{duration:180,iterations:Infinity});if(h.pulse)layer().animate([{transform:'scale(1)'},{transform:'scale(1.08)'},{transform:'scale(1)'}],{duration:700,iterations:Infinity});notify('Hover сработал')});root.addEventListener('mouseleave',()=>{if(!test)return;const config=getConfig();root.style.filter='';root.style.transform=`translate(${config.transform.x}px,${config.transform.y}px) scale(${config.transform.scale}) rotate(${config.transform.rotation}deg)`;layer().getAnimations().forEach(a=>a.cancel())});root.addEventListener('click',()=>{if(!test)return;const config=getConfig(),c=config.interactions.click;if(c.burst)burst(config.interactions.burst);if(c.flash)root.animate([{filter:'brightness(1)'},{filter:'brightness(3)'},{filter:'brightness(1)'}],{duration:300});if(c.bounce||c.squash||c.shake){let mid=c.shake?'translateX(8px)':c.squash?'scale(.82,1.18)':'translateY(-22px)';layer().animate([{transform:'none'},{transform:mid},{transform:'none'}],{duration:520,easing:'ease-out'})}notify('Click сработал')});return{setTest(v){test=v}}}
