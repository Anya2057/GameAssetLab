function bindFontWeight(layer){const font=FONT_MANIFEST.find(item=>item.name===layer.data.fontFamily)||SYSTEM_FONTS.find(item=>item.name===layer.data.fontFamily),weights=font?.weights||[400,700],button=editor.querySelector('[data-custom-font]');if(!button)return;button.insertAdjacentHTML('afterend',`<div class="control" style="margin-top:10px"><label>Насыщенность шрифта</label><select data-font-weight>${weights.map(weight=>`<option value="${weight}">${weight}</option>`).join('')}</select></div>`);const select=editor.querySelector('[data-font-weight]');select.value=layer.data.fontWeight;select.onchange=()=>{layer.data.fontWeight=+select.value;changed()}}
